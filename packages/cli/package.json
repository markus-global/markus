{
  "name": "@markus/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "markus": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b --watch",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@markus/comms": "workspace:*",
    "@markus/core": "workspace:*",
    "@markus/org-manager": "workspace:*",
    "@markus/shared": "workspace:*",
    "commander": "^14.0.3"
  }
}
