{
  "name": "markus",
  "version": "0.2.0",
  "private": true,
  "description": "AI Native Digital Employee Platform",
  "license": "Apache-2.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "clean": "pnpm -r clean"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "dependencies": {
    "@types/node": "^25.3.0",
    "drizzle-orm": "^0.45.1",
    "postgres": "^3.4.8",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.9"
  }
}
