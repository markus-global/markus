name: Publish

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write
  packages: write

jobs:
  publish-npm:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          registry-url: 'https://registry.npmjs.org'

      - run: pnpm install --frozen-lockfile

      - name: Build all packages
        run: pnpm build

      - name: Build Web UI
        run: pnpm --filter @markus/web-ui build

      - name: Bundle CLI for publishing
        run: pnpm --filter @markus-global/cli build:bundle

      - name: Verify bundle
        run: |
          ls -lh packages/cli/dist/markus.mjs
          ls -d packages/cli/dist/web-ui || echo "Warning: Web UI not bundled"
          ls -d packages/cli/templates || echo "Warning: templates not bundled"

      - name: Publish to npm
        run: cd packages/cli && npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

  # publish-docker:
  #   runs-on: ubuntu-latest
  #   needs: publish-npm
  #   if: ${{ secrets.DOCKERHUB_TOKEN != '' }}
  #   steps:
  #     - uses: actions/checkout@v4
  #     - name: Extract version from tag
  #       id: version
  #       run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> "$GITHUB_OUTPUT"
  #     - uses: docker/setup-buildx-action@v3
  #     - uses: docker/login-action@v3
  #       with:
  #         username: ${{ secrets.DOCKERHUB_USERNAME }}
  #         password: ${{ secrets.DOCKERHUB_TOKEN }}
  #     - uses: docker/build-push-action@v6
  #       with:
  #         context: .
  #         push: true
  #         tags: |
  #           markus/markus:${{ steps.version.outputs.VERSION }}
  #           markus/markus:latest
  #         cache-from: type=gha
  #         cache-to: type=gha,mode=max

  build-binaries:
    needs: publish-npm
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            platform: linux
            arch: x64
          - os: macos-latest
            platform: darwin
            arch: arm64
          - os: macos-13
            platform: darwin
            arch: x64
          - os: windows-latest
            platform: win
            arch: x64
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - name: Build all packages
        run: pnpm build

      - name: Build Web UI
        run: pnpm --filter @markus/web-ui build

      - name: Bundle CLI for publishing
        run: pnpm --filter @markus-global/cli build:bundle

      - name: Build binary archive
        run: bash scripts/build-binary.sh ${{ matrix.platform }} ${{ matrix.arch }}

      - name: Upload binary artifact
        uses: actions/upload-artifact@v4
        with:
          name: markus-${{ matrix.platform }}-${{ matrix.arch }}
          path: dist-binary/markus-v*.*
          retention-days: 5

  github-release:
    runs-on: ubuntu-latest
    needs: [publish-npm, build-binaries]
    steps:
      - uses: actions/checkout@v4

      - name: Extract version from tag
        id: version
        run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> "$GITHUB_OUTPUT"

      - name: Download all binary artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: List artifacts
        run: ls -lhR artifacts/ || echo "No artifacts"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: v${{ steps.version.outputs.VERSION }}
          generate_release_notes: true
          files: artifacts/*
          body: |
            ## Install

            **Zero prerequisites** (downloads include Node.js runtime):
            ```bash
            # macOS / Linux
            curl -fsSL https://markus.global/install.sh | bash

            # Windows (PowerShell)
            irm https://markus.global/install.ps1 | iex
            ```

            **Or via npm** (if you already have Node.js 22+):
            ```bash
            npm install -g @markus-global/cli@${{ steps.version.outputs.VERSION }}
            ```

            ## Direct Downloads

            | Platform | Download |
            |----------|----------|
            | Windows x64 | [markus-v${{ steps.version.outputs.VERSION }}-win-x64.zip](https://github.com/markus-global/markus/releases/download/v${{ steps.version.outputs.VERSION }}/markus-v${{ steps.version.outputs.VERSION }}-win-x64.zip) |
            | macOS Apple Silicon | [markus-v${{ steps.version.outputs.VERSION }}-darwin-arm64.tar.gz](https://github.com/markus-global/markus/releases/download/v${{ steps.version.outputs.VERSION }}/markus-v${{ steps.version.outputs.VERSION }}-darwin-arm64.tar.gz) |
            | macOS Intel | [markus-v${{ steps.version.outputs.VERSION }}-darwin-x64.tar.gz](https://github.com/markus-global/markus/releases/download/v${{ steps.version.outputs.VERSION }}/markus-v${{ steps.version.outputs.VERSION }}-darwin-x64.tar.gz) |
            | Linux x64 | [markus-v${{ steps.version.outputs.VERSION }}-linux-x64.tar.gz](https://github.com/markus-global/markus/releases/download/v${{ steps.version.outputs.VERSION }}/markus-v${{ steps.version.outputs.VERSION }}-linux-x64.tar.gz) |
