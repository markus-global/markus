node_modules/
dist/
*.tsbuildinfo
.env
.env.*
!.env.example
*.log
*.backup
.DS_Store
coverage/
coverage-*/
# 覆盖率报告备份/旧版目录（如 coverage.bak.77854 / coverage.old.78432）——
# 带点号命名不匹配上面的 coverage-*/，曾于 8202574b 误提交 158 个文件
coverage.bak.*/
coverage.old.*/
.turbo/
.markus/
.cursor/

# Build artifacts copied during bundle (packages/cli/build.mjs)
packages/cli/templates/

# Binary distribution build output (scripts/build-binary.sh)
dist-binary/

# Electron desktop build output (packages/desktop/electron-builder)
packages/desktop/dist-electron/
packages/desktop/node_modules/

# Config lock files
markus.json.lock

# Auto-generated API docs (regenerate via typedoc)
docs/api/
docs/api-test/

# Paper — independent repo (paper/.git)
paper/

# Locally downloaded Windows installers
Markus-Setup-*.exe
*.dmg
