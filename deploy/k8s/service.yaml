apiVersion: v1
kind: Service
metadata:
  name: markus-server
  namespace: markus
  labels:
    app: markus
spec:
  type: ClusterIP
  ports:
    - port: 3001
      targetPort: 3001
      name: api
    - port: 3002
      targetPort: 3002
      name: comm
    - port: 9000
      targetPort: 9000
      name: feishu-webhook
  selector:
    app: markus
    component: server
---
apiVersion: v1
kind: Service
metadata:
  name: markus-postgres
  namespace: markus
spec:
  type: ClusterIP
  ports:
    - port: 5432
  selector:
    app: markus
    component: postgres
---
apiVersion: v1
kind: Service
metadata:
  name: markus-redis
  namespace: markus
spec:
  type: ClusterIP
  ports:
    - port: 6379
  selector:
    app: markus
    component: redis
