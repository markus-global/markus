{
  "no-console": {
    "packages/core/src/coding-tools/adapters/cursor-agent-adapter.ts": "外部 CLI 适配器：把 exitCode + stderr 打到 stderr 是刻意的错误上报；该模块刻意不依赖 logger（避免适配器层耦合运行时）。",
    "packages/core/src/concurrent/state-manager.ts": "事件监听器内的兜底错误打印：监听器抛错无处上抛，打出来就是为了不静默吞掉。",
    "packages/core/src/concurrent/task-executor.ts": "任务进度事件监听器的兜底错误打印（同上，避免静默）。",
    "packages/core/src/concurrent/task-queue.ts": "任务回调（onStart/onComplete/onError/onProgress）的兜底错误打印：回调异常不能上抛打断队列。",
    "packages/core/src/llm/openai.ts": "provider 文件刻意保持不依赖 logger（模块头部有注释说明），warn 用于报告已恢复的异常工具调用。",
    "packages/org-manager/src/routes/skills.ts": "catch 内真实错误上报（fs 删除失败），不是调试残留。"
  },
  "no-empty-catch": {}
}
